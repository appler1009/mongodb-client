# Node.js dependencies
node_modules/
backend/node_modules/
frontend/node_modules/

# Production build output
backend/dist/
frontend/dist/
frontend/build/ # Common for some React setups

# Environment variables
.env
.env.local
.env.*.local
.env.development.local
.env.test.local
.env.production.local

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*

# Diagnostic reports (https://nodejs.org/api/report.html)
report.[0-9]*.[0-9]*.[0-9]*.[0-9]*.json

# Runtime data
pids
*.pid
*.seed
*.pid.lock

# macOS system files
.DS_Store

# IDE and Editor files
.vscode/        # VS Code
.idea/          # WebStorm/IntelliJ
*.iml
*.iws
*.ipr
*.sublime-project
*.sublime-workspace
.project
.settings
.factorypath
.classpath

# Docker-related files
*.env # Although we typically use .env for local, you might have other .env files you don't want committed.
docker-compose.override.yml # If you use an override for local dev that you don't want committed
.dockerignore # If you create separate .dockerignore files, you might want to ignore them if they're not intended for sharing
# For Docker volumes, if you were to define them directly in the root and not in `docker-compose.yml`
mongo-data/ # This is the named volume, docker manages it, no need to track its contents directly on host

# TypeScript build info
*.tsbuildinfo

# Test coverage
/coverage/

# Misc
.cache/
.npm
.yarn/
tmp/

# Project specific
backend/connections.json

